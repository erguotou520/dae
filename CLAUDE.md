# CLAUDE.md

本文档是 `dae` 仓库的开发约定与项目说明，供自动化协作、人工开发与代码审查共同遵循。

## 项目说明

`dae` 是一个 Linux 下的高性能透明代理方案，核心能力包括：

- 通过 eBPF / tproxy 接管流量
- 基于路由规则选择出口组与节点
- 支持订阅、分组、健康检查、DNS 转发与重写
- 支持 `dae reload` 进行平滑重载
- 以 systemd 方式部署时，日志通常通过 stdout/stderr 进入 journald

本仓库当前已内置控制台能力，控制台与代理核心运行在同一个进程内，避免外挂式 dashboard 在 reload、日志和状态同步上的问题。
控制台展示的数据应优先来自 `ControlPlane`、`DnsController`、`DialerGroup` 和显式事件上报，而不是通过外部日志回放或 journalctl 反推。

## 目录结构

以下目录是开发中最常接触的部分：

- `cmd/`
  - Cobra 命令入口，包含 `run`、`reload`、`validate`、`export` 等命令
  - `cmd/run.go` 是主进程启动和 reload 的核心入口
- `control/`
  - 控制平面、tproxy 监听、DNS 转发、TCP/UDP 处理、路由选择
  - 控制台需要读取的运行时状态主要来自这里
- `component/`
  - 节点、拨号器、分组、健康检查、过滤器等组件
- `config/`
  - 配置结构体、解析器、outline 导出、合并逻辑
- `common/`
  - 通用工具、常量、订阅解析、资源访问等
- `pkg/`
  - 日志、配置解析、ebpf 相关工具等内部包
- `internal/console/`
  - 内置控制台服务，包含 HTTP API、WebSocket、页面模板、日志快照与配置编辑入口

## 运行与入口

- `main.go` 只做基础初始化并调用 `cmd.Execute()`
- `cmd/run.go` 是 `dae` 的主入口
- 控制台必须在 `run` 命令中启动，并绑定到当前 `ControlPlane`
- reload 过程中会重建 `ControlPlane`，控制台必须能切换到新实例，不能持有过期引用

## 开发规范

### 1. 以控制平面为真源

控制台展示的分组、节点、拨号器、策略等运行时信息，应优先从 `ControlPlane` 或其下游组件获取，而不是依赖页面端猜测。

### 2. 运行时状态是事件源

流量记录、DNS 解析、连接记录、分组选路变化等信息，应从代码路径中的显式上报获取。

- TCP/UDP 选路后，直接上报 `src/dst/domain/network/outbound/dialer/policy/pid/pname/mac`
- DNS 请求选路后，直接上报 `qname/qtype/network/outbound/dialer/upstream`
- 分组与节点快照从 `ControlPlane.DashboardSnapshot()` 获取，节点列表必须合并订阅节点与运行时分组节点
- 日志只保留给“原始日志查看”能力，不再作为控制台统计源

### 3. reload 友好

- 任何新增状态都要考虑 reload
- 不要把 UI 或 API 状态绑死在旧的 `ControlPlane` 实例上
- 如果引入缓存，必须保证在 reload 后能刷新或失效

### 4. 接口设计

- API 返回值尽量稳定、可序列化、字段名清晰
- 对外输出的结构体字段使用 `json` 标签
- 错误信息要可读，并明确提示用户下一步怎么做

### 5. 配置文件约束

- 默认配置路径优先使用运行命令中的 `--config`
- 如果需要覆盖，允许使用环境变量 `DAE_CONFIG_PATH`
- 读不到配置时，控制台页面必须提示用户如何指定路径

### 6. 日志与状态约束

- 控制台页面的自动刷新必须避免覆盖用户正在编辑的配置
- 日志/连接/DNS 列表默认按时间倒序显示，最新内容优先
- 实时更新优先使用 WebSocket，其次再降级到轮询

### 7. 前端约束

- 控制台页面需要保持可读、简洁、明确的层级
- 深色模式、浅色模式、自动模式都要可用
- 不要引入复杂的前端构建链，优先使用单文件静态页面或极轻量的嵌入式资源
- 配置编辑器只保留一个可编辑区域，不要同时渲染多个编辑器

## 代码约束

- 避免在业务路径里引入无意义的全局变量
- 读取运行时状态时注意并发安全
- 新增公共方法前先确认它是否需要在 reload 场景下保持兼容
- 不要改写用户已经存在的配置行为，优先向后兼容
- 不要把控制台统计逻辑重新绑回日志解析或 journalctl 回放
- 新增状态请优先补到 dashboard snapshot 或显式 recorder，而不是补充文本日志格式

## 建议的修改顺序

1. 先补控制台所需的 snapshot 接口
2. 再接入日志存储与实时推送
3. 然后补 HTTP API
4. 最后再做页面细化与视觉优化

## 验证要求

每次修改控制台相关代码后，至少完成以下检查：

- `go test ./...` 或最小范围编译
- 运行 `dae run --config ...` 后检查控制台是否能启动
- 刷新页面确认：
  - 服务状态正常展示
  - 分组与节点数量正确
  - 配置文件可读写
  - 流量、DNS、连接记录可实时更新
  - reload 后页面仍然可用
